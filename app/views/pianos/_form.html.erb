<%= form_for(piano) do |f| %>
  <% if piano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(piano.errors.count, "error") %> prohibited this piano from being saved:</h2>

      <ul>
      <% piano.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--   <div class="field">
    <%= f.label :make %>
    <%= f.text_field :make %>
  </div> -->

  <div class="field">
    <%= f.select(:make, options_for_select(['Fender Rhodes', 'Wurlitzer', 'Clavinet'] )) %>
  </div>

  <div class="field">
    <%= f.text_field :model, placeholder: :model %>
  </div>

  <div class="field">
    <%= f.text_field :year, placeholder: :year %>
  </div>

  <div class="field">
    <%= f.text_field :description, placeholder: :description %>
  </div>

  <div class="field">
    <%= f.text_field :photo, placeholder: :photo %>
  </div>

  <div class="field">
    <%= f.text_field :price, placeholder: :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>